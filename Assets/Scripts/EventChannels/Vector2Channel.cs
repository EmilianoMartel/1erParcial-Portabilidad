using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace EventChannel {
    [CreateAssetMenu(fileName = "Vector2Channel", menuName = "EventChannel/Vectr2Channel")]
    public class Vector2Channel : EventChannels<Vector2>{ }
}